- Naprawiłam auth `gog` dla Google Calendar/Gmail przez remote flow bez SSH: `gog auth add u6133809438@gmail.com --remote --step=1`, Adam otworzył URL u siebie, wkleił końcowy localhost callback URL, potem `--step=2` zapisał token.
- Zweryfikowane, że token istnieje jako `token:default:u6133809438@gmail.com`, a `gog auth list` pokazuje konto `u6133809438@gmail.com` z usługami `calendar,gmail`.
- Potwierdzony właściwy rodzinny kalendarz Google: `hp0gd751p5t7kf1uh2ub76ehv4@group.calendar.google.com`, nazwa `Kalendarz Patrycji i Adama`.
- Na przyszłość dla dashboard-only VPS nie wracać do `gog auth login` z localhost browser flow, tylko od razu używać `gog auth add ... --remote` i poprosić Adama o zwrotny URL callback.
