- Reviewed `Brudnopis.md` in BIGAI as Adam’s reference for nonstandard Markdown patterns.
- Saved durable note: use BIGAI `Brudnopis.md` as first-stop source for hidden comments (`[text]: #`), reference links, footnotes, strikethrough, highlight, and Markdeep trailer examples.
- Adam explicitly wants this persisted across `/new`; when I add memory/skill learnings for this kind of workflow, also commit/push the workspace repo so the update survives resets.
