- Practical myclaw model workaround confirmed by Adam: `openclaw config set agents.defaults.model.primary "openai-codex/gpt-5.4"` appears to trigger a short micro-reload/reconnect that is enough to update the effective default model without a full dashboard restart. After running it, verify with `openclaw models`; if it shows `Default: openai-codex/gpt-5.4`, that is sufficient for day-to-day use.
- Working SOP for model drift after updates/restarts: (1) run `openclaw models`, (2) if default reverted to `openai/gpt-5.4`, run `openclaw config set agents.defaults.model.primary "openai-codex/gpt-5.4"`, (3) verify again with `openclaw models`. Treat full dashboard restart as separate and only needed when broader gateway/plugin state must reload.
- Adam confirmed the canonical plwiki article repo is `https://github.com/akumiszcza/plwiki-sandbox` with local clone at `/home/ubuntu/.openclaw/plwiki-sandbox`; before every translation-related action there, pull first, and after each meaningful change commit/push promptly.
- Pulled/cloned `plwiki-sandbox` locally today and verified `PL/Marry-your-rapist law.mediawiki` exists there.
- New plwiki durable note from repo `Linki.md`: use `{{Biblia}}` for Bible-passage citations; `{{Odn}}` remains historical but should now be treated as a gradually replaced pattern where native Cite sub-references (`<ref name="..." details="..." />`) work cleanly; propagate article-repo `Linki.md` additions back into the reusable plwiki skill notes.
- Confirmed working sub-reference pattern from Adam's live fix on `Marry-your-rapist law`: full source defined once in `<references>`, plain reuse as `<ref name="Scholz2021" />`, page-specific reuse as `<ref name="Scholz2021" details="s. ..." />`, and no second full definition of the same named ref in article text.
- Important contact preference: when Adam asks for report/link emails, default target address is `akumiszcza@gmail.com`; I mistakenly sent one report to the sending account mailbox first, so this should be treated as a durable routing rule.
- `Marry-your-rapist law` looks finished from Adam's perspective before `/new`: live working file confirmed as `PL/Marry-your-rapist law.mediawiki`; article repo got another archive pass (`7cfea09`) and `Linki.md` gained `Szablon:Link-interwiki` (`84fc8bb` in article repo, `964fd84` in skill repo).
- Durable plwiki workflow reminder: before article work read `/home/ubuntu/.openclaw/plwiki-sandbox/Linki.md`, pull first, commit/push after meaningful changes, re-confirm the live target file after pulls, and sync useful `Linki.md` additions back into `/home/ubuntu/.openclaw/workspace/skills/plwiki-sandbox-skill/Linki.md`.
