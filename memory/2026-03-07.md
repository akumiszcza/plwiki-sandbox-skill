# 2026-03-07

- Set up Gmail ingestion for u6133809438@gmail.com via OpenClaw Gmail Pub/Sub hook + gogcli + gcloud + Tailscale Funnel. Notes:
  - Tailscale Funnel had to be enabled in tailnet UI.
  - Needed `sudo tailscale set --operator=ubuntu` to allow non-root serve/funnel config.
  - gogcli built from source (Go installed) and installed to /usr/local/bin; symlinked to /usr/bin/gog so gateway can find it after restarts.
  - gog keyring uses file backend with passphrase; for headless runs require `GOG_KEYRING_PASSWORD` env.
  - Gateway restarts can stop gmail-watcher if tailscaled/gog not running; re-start tailscaled (userspace networking) and `openclaw webhooks gmail run`.

- Travel logging workflow established:
  - On each relevant forwarded email, reply with (1) short summary, then (2) a single formatted line starting with lot:: / hotel:: / pociąg::.
  - Maintain workspace files, sorted chronologically: flights.md, hotels.md, trains.md.
  - Preferences: seat notation compact like {27ABC}, no per-person mapping; in formatted lines use commas (no em dash) and do not mention "czas lokalny".

- Telegram added as primary chat surface:
  - Created bot @Sarah_MyClaw_bot; configured `channels.telegram.enabled=true`, `dmPolicy=pairing`, and paired Adam (telegram user id 8577234145).
  - Hook delivery was initially routed to dashboard due to `channel: last`; fixed by pinning Gmail hook mapping to `channel: telegram`, `to: 8577234145`, plus a WhatsApp mirror.
  - Disappearing Telegram messages correlated with gateway restarts mid-reply.

- Model config/cost tuning:
  - Attempted openai/gpt-5.4 as default; OpenClaw threw "Unknown model: openai/gpt-5.4". OpenAI API responds OK for gpt-5.4, but OpenClaw still errored.
  - Added model allowlist entries/aliases. Ultimately set default model to openai/gpt-5-mini with fallback openai/gpt-5.2 to reduce cost (gpt-5.2-mini does not exist).

---

Additional notes (new, appended now):

- Today processed these two forwards most recently:
  - Apartments Tina (Booking ref 5125455949) — added to hotels.md as 2026-07-03T12:00/2026-07-04T11:30 Apartments Tina, Chiou 7, Spata, GR, 2 rooms, 5 adults, €175.89, FREE transfer. (Did not store PIN or card number in canonical line.)
  - SKY express (S7RUQ0) — added to flights.md as 2026-07-04T12:30/2026-07-04T13:10 ATH ⇒ JNX GQ402 ref S7RUQ0 {5A,5B,5C,5D,6A}, 1x23kg + 8kg + przedmiot osobisty per pax, €533.10.

- WhatsApp gateway experienced transient disconnects (stale-socket) around 09:29 and creds.json was restored from backup at ~10:00; channel restarted and is currently listening again. Messages sent in the 09:29–10:00 window may have been missed; manual resend requested.

- Next actions queued for user confirmation:
  1. rclone OneDrive authorize (user must run on macOS) and then I will configure remote and run dry-run + copy.
  2. Security hardening changes (chmods, gateway UI flags) — require explicit OK.
  3. Persist GOG keyring passphrase securely for unattended reboots (design pending user decision).


- Recent changes (appended now):
  - Removed Wizz Air BQH3GX (refund requested by user) and updated flights.md counter.
  - Added LOT LO303 YTORXE: lot:: 2026-07-19T15:35/18:05 (2h30m) **WAW ⇒ FCO** LO303 (LOT) ref YTORXE {26A}, 23kg + 8kg + osobisty, 530,03 PLN.
  - Bolded airport arrow formatting (**AAA ⇒ BBB**) across flights.md for better Obsidian rendering.

Timestamp: 2026-03-07T17:11:00Z
